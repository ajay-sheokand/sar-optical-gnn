# Pinned to what's installed and verified working in venv/ (Python 3.14.4) as of M0.
# Grouped by *when* each dependency is actually needed, not alphabetically — so it's clear which
# milestone (docs/RESEARCH_PLAN.md §7) pulls each one in, rather than installing everything for
# every stage of the project up front.

# --- Core (already used by data_loader.py and src/graph_builder.py) ---
torch==2.12.1
torch-geometric==2.8.0
earthengine-api==1.7.33
xarray==2026.4.0
rasterio==1.5.0
numpy==2.5.0
pandas==3.0.3
matplotlib==3.11.0
scikit-image==0.26.0
networkx==3.6.1

# --- M0: testing ---
pytest==9.1.1

# --- M1: dataset loaders (BigEarthNet via torchgeo, SEN1-2/SEN12MS custom loaders) ---
# Not yet installed — torchgeo pulls in a large tree (geopandas, pytorch-lightning, kornia, timm,
# segmentation-models-pytorch, ...). Verified during M0 that all of it has working Python 3.14
# wheels (no source builds required), but left uninstalled until M1 actually needs it, so M0's
# environment stays light.
# torchgeo==0.9.0

# --- M5: evaluation metrics (SSIM/PSNR/LPIPS/FID) ---
# Also verified for Python 3.14 wheel availability during M0, left uninstalled until M5.
# lpips==0.1.4
# torch-fidelity==0.4.0
# torchmetrics==1.9.0
