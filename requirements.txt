# Pinned to what's installed and verified working in venv/ (Python 3.14.4) as of M0.
# Grouped by *when* each dependency is actually needed, not alphabetically — so it's clear which
# milestone (docs/RESEARCH_PLAN.md §7) pulls each one in, rather than installing everything for
# every stage of the project up front.

# --- Core (used by src/datasets/delhi_gee.py and src/graph_builder.py) ---
torch==2.12.1
torch-geometric==2.8.0
earthengine-api==1.7.33
xarray==2026.4.0
rasterio==1.5.0
numpy==2.5.0
pandas==3.0.3
matplotlib==3.11.0
scikit-image==0.26.0
networkx==3.6.1

# --- M0: testing ---
pytest==9.1.1

# --- M1: dataset loaders ---
# torchgeo backs src/datasets/bigearthnet.py and src/datasets/sen12ms.py (SEN1-2 in
# src/datasets/sen1_2.py is a from-scratch loader, no torchgeo class exists for it). Installing
# torchgeo also pulls in a large tree (geopandas, pytorch-lightning, kornia, timm,
# segmentation-models-pytorch, torchmetrics, torchvision, ...) — all verified for Python 3.14
# wheel availability during M0, and now actually installed as of M1.
torchgeo==0.9.0
torchvision==0.27.1
# scipy and Pillow: needed by src/datasets/sarptical.py (a real dataset downloaded and validated
# end-to-end after M2 — see docs/BUILD_LOG.md — before committing to BigEarthNet's much larger
# download). scipy.io.loadmat reads the dataset's .mat SAR patches; Pillow reads its .png optical
# patches. scipy already arrived transitively (via rasterio), Pillow via torchvision; pinned
# explicitly here since this project's own code imports both directly, not just transitively.
scipy==1.18.0
pillow==12.3.0

# --- M3: baseline evaluation metrics (SSIM/PSNR/FID) ---
# torchmetrics arrived as a torchgeo dependency already (pinned above at the version torchgeo
# pulled in). torch-fidelity backs torchmetrics' FID implementation (InceptionV3 features) --
# needed now, not deferred to M5 as originally planned, because the SEN1-2 validation pass (§4)
# itself checks our FID numbers against the literature's, not just PSNR/SSIM.
torchmetrics==1.9.0
torch-fidelity==0.4.0

# --- M5: perceptual loss/metric ---
# lpips isn't needed for M3's baselines (classic pix2pix/CycleGAN train on L1 + adversarial only,
# no perceptual term) -- deferred until M4's graph-hybrid model, which the plan says uses it.
# lpips==0.1.4
